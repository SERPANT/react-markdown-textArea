import React from 'react';
import ReactDOM from 'react-dom';

import MarkDownTextField from './MarkDownTextField';

ReactDOM.render(
  <MarkDownTextField placeHolder="hello hello " />,
  document.getElementById('root')
);
